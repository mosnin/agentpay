# Local PostgreSQL for Bids.
#   docker compose up -d
#   npm run db:push && npm run db:seed && npm run dev
# Matches the default DATABASE_URL in .env.example.
services:
  db:
    image: postgres:16
    container_name: bids-pg
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bids
    ports:
      - "5432:5432"
    volumes:
      - bids-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d bids"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  bids-pgdata:
