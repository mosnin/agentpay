# Production activation — September 7, 2026

PRs #18, #19 and #20 are merged. The accepted production revision is `2d4e934a7611f5799691d4ebde9762090fb3582e`; Vercel deployment `dpl_9mSgrKsWPzNg92tdZw4PoFW6L7ZA` serves bids.sh and www.bids.sh. All four CI jobs passed on that exact merge revision. This supersedes the preview-only deployment status in the earlier release report.

The actual Neon main branch was backed up and restored to an isolated local database. Its current drift exactly matched the reviewed additive observed-schema upgrade. That upgrade, baseline resolution, and all ten migrations passed on the restored copy before running on production. The final production schema matches Prisma; original User, Agent, Task, Payment and Artifact record identities and counts were preserved. The backup is private, outside Git. Backup SHA-256: `a1822babebbfc22c9ecb01855a3f371cb9ac94ad7c93f68f2a8b1e5adf86246a`.

Production uses shared PostgreSQL rate limits. The GitHub operations recovery workflow successfully invoked both jobs against www.bids.sh and their durable production runs completed. Automatic maintenance is configured in `vercel.json`: settlements every five minutes, verification offset by two minutes. Vercel supplies the configured CRON_SECRET; the existing handlers reject unauthorized calls and hold durable leases. GitHub remains a manual recovery trigger. The first GitHub scheduled run was not observed, so that scheduler is not retained as the production dependency. Vercel registered both definitions against the deployment above. With no manual calls after deployment, settlements completed automatically at 2026-09-08 00:05:27 UTC and verification at 00:07:40 UTC; both durable OperationRun records are succeeded.

Clerk's existing Bids production application now has a lifecycle endpoint at `https://www.bids.sh/api/webhooks/clerk`, subscribed only to `user.updated` and `user.deleted`. The endpoint is `ep_3J1KfqML3zoJsGebmpIIOhJ3bfV`. Its signing secret is stored as a production Vercel secret. Signed provider sample update `msg_3J1OEa98PRGdo2nHzFwNwho8V8J` and deletion `msg_3J1OnsC7c839GUbo74oW6CGE11j` both succeeded. Both sample user IDs were absent from production, so no customer account was deleted or changed. Unsigned requests return HTTP 400. These samples verify delivery and handler acceptance, not actual customer deletion.

Normal owner email-code sign-in was exercised against www.bids.sh. A temporary brief saved to the account, survived a full reload, and was discarded. Database checks confirmed one user, zero tasks, zero payments and zero remaining briefs. No paid task or catalog fixture was created.

The follow-up dependency patch pins deepmerge-ts 8.0.2, addressing [GHSA-ggr8-5vv4-36mx](https://github.com/RebeccaStevens/deepmerge-ts/security/advisories/GHSA-ggr8-5vv4-36mx). Prisma uses its plain deepmerge export for configuration loading; client generation, migration status, typecheck and all 307 unit tests pass with the override. CI now rejects high as well as critical runtime advisories. The audit has zero high/critical and 23 moderate findings; it is not an independent security audit.

Payments remain explicitly disabled in production. Public Base Sepolia contract acceptance has now passed; the [receipt](../testnet/base-sepolia-receipt.json) records escrow release/refund/dispute and native-USDC instant split, with exact measured balances and three-confirmation canonical receipts. These are actual public testnet transactions, not mainnet or a hosted buyer-to-seller application journey. Mainnet treasury configuration and a persistent hosted seller have not been established. The generated addresses and 5% fee are test-only. Ethereum, Solana and Robinhood settlement activation is not established by the Base tests.

Reproducible receipts and private-backup handling scripts for this activation are under `/Users/preston/bids-product-evidence/production-upgrade*` and `activation-*` on the operator host. No customer credentials are committed.
